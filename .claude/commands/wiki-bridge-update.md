Record a bridge entry per §6.2 of CLAUDE.md — factual record that Luna actually used a wiki concept in project work.

Project: $ARGUMENTS

Ask Luna (if not already stated):
- Which project deliverable / section was produced
- Which wiki concepts fed into it
- Date of use

Append or update `project-bridges/<project>.md` with:
- Forward mapping: deliverable → wiki concepts (with date)
- Reverse index: wiki concept → this usage

Do NOT write back to the project folder. Bridges live wiki-side only.

Do NOT speculate — only record what Luna confirms. If she hasn't confirmed usage, don't write.
